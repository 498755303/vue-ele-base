const user = {
    state: {
        userId: '',
    },

    mutations: {
        setUserId: (state, id) => {
            state.userId = id;
        }
    },

    actions: {

    }
};
export default user;
